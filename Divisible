#include<stdio.h>
#include<stdbool.h>
#include<malloc.h>

char* solve (int* A, int N ) {
   // Write your code here
}

int main() {
    int N;
    scanf("%d", &N);
    int i_A;
    int *A = (int *)malloc(N*sizeof(int));
    for(i_A=0; i_A<N; i_A++)
    	scanf("%d", &A[i_A]);

    char* out_ = solve(A);
    printf("%s", out_);
}
